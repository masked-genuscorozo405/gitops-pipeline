apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitops-pipeline

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: gitops-pipeline

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

images:
  - name: ghcr.io/vinicius-costa14/gitops-pipeline
    newTag: latest
